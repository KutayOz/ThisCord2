// Application layer - contains interfaces and DTOs only
// Service implementations are in Infrastructure layer
namespace ThisCord.Application;

public static class DependencyInjection
{
    // No services to register in Application layer
    // All service implementations are registered in Infrastructure
}
